services:
  - type: web
    name: aswathama-classes
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 60 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
